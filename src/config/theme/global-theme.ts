import {StyleSheet} from 'react-native';

export const globalTheme = StyleSheet.create({
  globalMargin: {
    marginHorizontal: 20,
  },
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
